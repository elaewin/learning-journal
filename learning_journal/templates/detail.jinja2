{% extends "layout.jinja2" %}
{% block body %}
<article>
  <h2>{{ entry.title }}</h2>
  <hr />
  <p>{{ entry.body }}</p>
  <hr />
  <p>Created: <strong title="{{ entry.created }}">{{entry.created}}</strong><br>
  Edited: <strong title="{{ entry.edited }}">{{entry.edited}}</strong></p>
{#   {% if entry.edited %}
    <p>Edited <strong title="{{ entry.edited }}">{{entry.edited}}</strong></p>
  {% else %}
    <p>Created <strong title="{{ entry.created }}">{{entry.created}}</strong></p>
  {% endif %} #}
  <p><a href="{{ request.route_url('action', action='edit', _query=('id, entry.id)) }}">Edit Entry</a></p>
</article>
{% endblock %}